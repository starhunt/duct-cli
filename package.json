{
  "name": "duct-cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "duct": "./src/cli.ts"
  },
  "scripts": {
    "start": "bun run ./src/cli.ts",
    "build": "bun build ./src/cli.ts --compile --outfile dist/duct-macos",
    "typecheck": "bun x tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "@types/bun": "^1.2.0"
  }
}
